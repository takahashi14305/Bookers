<h1>Books</h1>
<%= form_with_model: @book.each do |f

<%= @books.each do |book| %>

  <p>title</p>
  <span><%= book.title %></span>
<% end %>
  <p>body</p>
  <span><%= book.title %></span>
  
<% end %>



  <h2>New book</h2>
  <h4>Title</h4>
  <%= f.text_field :title %>
	
  <h4>Body</h4>
  <%= f.text_area :body %>
	
  <%= f.submit 'Create Book' %>
<% end %>