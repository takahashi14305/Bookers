<h1>Books</h1>
<% @book.each do |book| %>

　<p>title</p>
  <span><%= book.title %></span>
  
  <p>body</p>
  <span><%= book.body %></span>
  
  <span><%= link_to "show",book_path(book.id) %>

  <% end %>



  <h2>New book</h2>
  <%= form_with model: Book.new do |f| %>

  <h4>Title</h4>
  <%= f.text_field :title %>

  <h4>Body</h4>
  <%= f.text_area :body %>

  <%= f.submit 'Create Book' %>
<% end %>